#include<stdio.h>
#include<stdlib.h>
#include "SuperMarket.h"

int main()
{
    makeHeap(items);
    int size = 5; // Hardcoding the number of products to ease with heap creation.
    menu(size);
    
}
